{
  "note": [
    "Sentences where the comma before and/or joins clauses or a two-item pair",
    "rather than closing a series, which the house style keeps. The linter cannot",
    "tell those two shapes apart, so each accepted sentence is recorded here",
    "verbatim. Rewriting a sentence retires its entry and the check asks again,",
    "which is the point. Read the sentence before you add it: if the comma is",
    "closing a list of three or more, the fix is to delete the comma, not to land",
    "here. A sentence that quotes the serial comma to talk about it also lands",
    "here, because the quote has to stay wrong to make its point.",
    "Regenerate candidates with: node scripts/lint-prose.mjs --json"
  ],
  "allow": [
    "So immediately before stage 4, the pipeline drops an empty schematic, a blank board with a default outline and a project file into place, and wires them into the config.",
    "A failed attempt's rollback deletes it, because at that point it is still untracked, and a retry against a missing schematic fails worse than the failure it was recovering from.",
    "Wait for the reset, run the same command, and the completed turns replay at roughly zero tokens.",
    "On one recorded run, the schematic stage took 65 minutes, and five of its forty turns emitted 89% of the run's 170,920 output tokens.",
    "The two older control boards came back byte-identical, so nothing regressed, and the buck converter is now a third pinned reference.",
    "It has local repair moves, the router tries several trunk positions before falling back to labels, and a colliding label will walk itself outward along its stub, but there is no draft-check-redraft cycle inside the engine.",
    "The draft layout is correct, not optimal, and it says so itself, in a that lists exactly what a specialist should redo before fab.",
    "The draft layout is correct, not optimal, and it says so itself.",
    "Edits are targeted changes to the s-expression source, which means your diffs stay readable and reviewable, and the parts of the file nobody asked about stay byte-identical.",
    "Every claim made elsewhere here is a claim about behaviour, that the agent propagates a change across every artifact referencing it, that it refuses a request which breaks a recorded budget and cites the arithmetic, and none of that can be checked by reading the tool.",
    "The alternative was a checker function per task, which is what most agent benchmarks do, and it was rejected.",
    "(a change satisfying every recorded constraint exists), (no change does, so the correct behaviour is to decline and show the arithmetic) or (the repository is internally inconsistent in a way that reflects a requirement violation, and the correct behaviour is to surface both sides without resolving either).",
    "Cost is computed from a dated, pinned price table, and the record stores both the figure and the table version so a later price change never rewrites history.",
    "A model whose score collapses on variants is recalling, not reasoning, and that becomes a number instead of a suspicion.",
    "is the bare slug, carrying no directory, no theme suffix and no extension, and in [src/blog.ts resolves it to the pair: A naming a card nobody rendered throws at build time rather than shipping a hole, so render before you write the frontmatter.",
    "- is prose, two to five sentences, and it says why the role exists rather than what the company does.",
    "- [form-founding-ai-engineer.md , live at [binary.so/ejd7Mkv , and the live form is behind the spec",
    "Publish a fourth role, forget the option, and every application for it arrives labelled as something else.",
    "Stateful execution loops that plan work, call tools, inspect results and recover are the first thing on the listing's work list, and someone who has built one has an answer to \"how did it decide it was done\" that a demo does not.",
    "copperhead is free to install and runs locally, so trying it costs a candidate ten minutes, and asking what they made of it is both a filter and the best product feedback in the pile.",
    "It is deliberately not , because a fixed height plus silently shears off whatever sits at the bottom, and what sits at the bottom is the live-stats row.",
    "they say what the repository is, which is a different job from what the product is, and a reader on a file listing wants the former.",
    "The hero subhead is the one-liner with the Oxford comma dropped from \"design, verify, and ship\", and that is deliberate too.",
    "- **\"ERC/DRC don't catch everything — what about signal integrity, EMC, thermal?\"** (Himanshu) - Correct, and neither does a human at schematic stage.",
    "The serial comma test is a heuristic, because \"x, and y\" is a serial comma only when the sentence is running a series.",
    "Served raw from public/ the browser was downscaling 2400px into a 405px grid cell, a 5.9x reduction it does with a cheap filter, and the card text went soft.",
    "* requires one of the two, so that posting is invalid, and nothing on the page",
    "An interest in electronics, hardware design or PCB engineering, and the ability to pick up an unfamiliar technical domain quickly.",
    "a degree in computer science, engineering or a related field is welcome, and evidence that you have built and trained these systems counts for just as much.",
    "A degree in computer science, engineering or a related field is welcome, and equivalent practical experience counts for just as much.",
    "* has, the navbar, the index, the sitemap and llms.txt, and emits no",
    "Marks are the official brand paths — simple-icons (CC0) except Sarvam's, which simple-icons has none of and which comes from Sarvam's own SVG — drawn in currentColor so the whole row is one ink and reads as a set instead of eight competing brand palettes — the standard treatment for a partner strip, and the only one that survives this site's light and dark themes.",
    "It is a band, not a section, and the page it sits in knows better than it does how much air belongs above and below — the hero has a fold to fit it into.",
    "It is also by far the longest path in the set, at roughly four times the AWS wordmark, and the template repeats every mark fourteen times.",
    "compression collapses the repeat almost entirely, so what is left is the price of the path once, and hoisting the marks into a <symbol> to dedupe them by hand would buy back almost nothing on the wire.",
    "drop below about five backers, or shorten the labels much, and one pass falls under 1328 again and the seam opens.",
    "` label, which lets a pipeline transcript state a stage's outcome in one row instead of a stage-running row followed by a done row — half the rows, and on a phone, where the window's height is the only free dimension, half the height.",
    "The SVG is built here, at build time, and shipped as markup — the same no-client-JS bargain the rest of the site makes.",
    "The detail those would surface is carried instead by the peak label, the axis ticks and the full PDF report linked at the foot of the page, and the SVG gets a title/desc pair so a screen reader is told the shape rather than shown an unlabelled graphic.",
    "* Headline number for the tile, already reduced (a sum, or a latest value).",
    "A hardware design spreads one decision across a schematic, a bill of materials, a power budget and several documents, and nothing breaks when they fall out of sync.",
    "The gerbers are producible and the layout is DRC-clean, but the draft layout is correct rather than optimal, and it says so itself.",
    "The unfurl gets it from og:image above; this is the same file as an absolute URL for the BlogPosting node, because a crawler reading the structured data does not look at the meta tags, and a post whose card and whose schema image disagree is a post with two different thumbnails depending on who is asking.",
    "A repository, a board you have built or a design you have argued with beats a covering letter, and it is what we would ask for next anyway.\"} </p> { /* A role that names what an application must carry gets a list, not a longer sentence.",
    "Kept to four, one sentence or two each, and each one is a thing that is already true rather than a value we would like to have.",
    "it has no responsibilities, nothing to verify and no date, and a fabricated Role would sit one `posted` field away from emitting JobPosting markup for a job that does not exist.",
    "The owner/repo prefix is the same on every row, so it is noise in a table about one repo — strip it, and give the bare repo root a name of its own.",
    "Stars, forks and the two snapshot tables are each allowed to be missing on their own — ghrs only writes forks.csv once there is a fork, and stargazers.csv once there is a star — but with no traffic series there is no page.",
    "* 90-day uptime, 0..100, or null when nothing was measured",
    "Space Grotesk throughout — display and text both — with JetBrains Mono for code, the wordmark and reference numbers, and DM Sans in the nav bar alone.",
    "both keywords, so light-dark() resolves off the OS preference until the toggle pins it, and so UA chrome (scrollbars, form controls, the caret) follows the theme it is painted on",
    "This comment used to claim 500, which was simply wrong, and the page was rendering a weight light of the reference everywhere.",
    "The wrapper starts below the hero, because on their page the frame does too — the hero is the page's cover, not a cell of the grid, and ruling its flanks would fight the dot field already there.",
    "A bare URL in a references list is one unbreakable word, up to 580px of it, and on a phone it pushed the whole document sideways.",
    "It works on your KiCad files, in your repo, and nothing is done until the checks pass.",
    "**Footnote:** The draft layout is correct, not optimal, and says so itself.",
    "copperhead is Apache-2.0, built on an open stack (KiCad, kicad-cli, OpenSpec), and everything it writes is plain markdown and JSON in your own repository.",
    "This is a Docker action, so every run builds a Dockerfile from whatever the ref resolves to, and it runs with a token that can read traffic for the product repo and push branches here."
  ]
}
