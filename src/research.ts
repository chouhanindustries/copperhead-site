/** Shared helpers for the research routes. Dates format via `formatDate` in ./blog. */

/** Canonical path for a research article, from its collection id (the filename slug). */
export const articlePath = (id: string) => `/research/${id}/`;
